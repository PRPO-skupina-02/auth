apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: auth
  namespace: production
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  tls:
  - hosts:
    - auth.cinecore.timhrovat.com
    secretName: auth-tls
  rules:
  - host: auth.cinecore.timhrovat.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: auth
            port:
              number: 8080
